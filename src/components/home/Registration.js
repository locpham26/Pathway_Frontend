import React from "react";
import styled from "styled-components";

const StyledRegistrationSection = styled.section`
  background-color: var(--primary-yellow);
  width: 100%;
  padding: 40px;
`;

const Registration = () => {
  return <StyledRegistrationSection></StyledRegistrationSection>;
};

export default Registration;
